[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  # Replace with your real backend URL after Render deploy.
  VITE_API_BASE = "https://your-render-backend-url.onrender.com"

